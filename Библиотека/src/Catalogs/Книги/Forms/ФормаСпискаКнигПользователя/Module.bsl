
&НаКлиенте
Процедура ПриОткрытии(Отказ)
	Абонент = ПолучитьТекущегоПользователя();
	//Период в запросе
	Список.Параметры.Элементы[0].Значение = ТекущаяДата();
	Список.Параметры.Элементы[0].Использование = Истина;
	//МестоРазмещения в запросе
	Список.Параметры.Элементы[1].Значение = Абонент;
	Список.Параметры.Элементы[1].Использование = Истина;
КонецПроцедуры

&НаСервере
Функция ПолучитьТекущегоПользователя()
	Возврат ПараметрыСеанса.ТекущийПользователь.Абонент;			
КонецФункции